services:
  postgres:
    image: postgres:16
    container_name: money-postgres
    environment:
      POSTGRES_DB: money
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 2552
    ports:
      - "5555:5432"
    volumes:
      - money-pgdata:/var/lib/postgresql/data

volumes:
  money-pgdata: